local M = {}

M.defaults = {
  duration_ms = 4000,
  width_ratio = 0.5,
  height_ratio = 0.5,
  audio = true,
  keymap = "<leader>yay",
  auto_close = false, -- Set to true to auto-close after duration_ms
}

return M
